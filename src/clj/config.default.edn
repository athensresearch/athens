{:http       {:port 3010}
 ;; Default fluree address and datascript path on dev setup.
 ;; The official Docker Compose file overwrites these values.
 :fluree     {:servers ["http://localhost:8090"]}
 :datascript {:persist-base-path "./athens-data/persist/"}
 :in-memory? false
 ;; :password   "SuchWow"
 ;; :nrepl      {:port 8877}
 }
