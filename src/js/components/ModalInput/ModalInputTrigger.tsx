import { PopoverTrigger } from "@chakra-ui/react";
import React from "react";

/**
 * ModalInputTrigger
 * The trigger for the ModalInput component.
 * @param {React.ReactChild}
 * @returns {React.ReactNode}
 */
export const ModalInputTrigger = ({ children }) => <PopoverTrigger>{children}</PopoverTrigger>;
