import { expect } from '@playwright/test';
import { test } from './electron-test';

test('new-test-template', async ({ page }) => {
  // Start the inspector to record steps manually.
  // Replace the filename, test name with the test name, and body of this test with the recorded steps.
  await page.pause();
});
